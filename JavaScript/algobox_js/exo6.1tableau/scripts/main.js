// on déclare un tableau vide qui contiendra 7 éléments
let tableau=new Array(7);

// pour chaque élément de mon tableau (via l'index) on lui met une valeur qui correspond à l'index
for(i=0; i < tableau.length; i++){
    tableau[i]=0;
}
alert(tableau);

